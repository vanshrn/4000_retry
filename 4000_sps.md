config.h:
#define SAMPLE_RATE     2000    change to 4000
#define WINDOW_SIZE     2000    change to 4000
#define ANALYSIS_WINDOW_SIZE    10000   change to 20000     (5s window)
#define ADS1292_CONFIG1_VAL     0x04    change to 0x05        (0x04 = 2000 sps, 0x05 = 4000 sps)
#define BL_STAGE1_WIN       400     change to 800       (800/4000 = 0.2 = 200 ms)
#define CALIBRATION_WINDOW_SAMPLES      10000   change to 20000     (5s @ 4000 SPS)
#define CALIBRATION_MIN_SAMPLES      4000   change to 8000      (2s @ 4000 SPS)



ble_transport.cpp:
vTaskDelay(pdMS_TO_TICKS(20));      change to 10    (transmission under 1sec)



ecg_pipeline.cpp:
applymedianspike:
if (d1 > 25000 && d2 > 25000)       change to 12500     (@ 4000 sps it takes half the time)

baselinewander:
const float R = 0.9985f;        change to 0.99925       @ 2000 (fc = (1 - 0.9985)/2Pi * 2000)   @4000 ((1 - 0.99925)/2 = 0.00075)       so R = 1 - 0.00075 = 0.99925

static float s_fir_notch_prev[40];      change to 80        (4000 / 50 = 80)    80 taps

applyFIRnotch:
for (int k = -19; k <= 20; k++)     change to -39 and 40        (80 taps range)
val = s_fir_notch_has_prev ? s_fir_notch_prev[40 + idx] : data[0];      change from 40 to 80
ma50 /= 40.0f;      change to 80
if (len >= 40)      change to 80
memcpy(s_fir_notch_prev, data + len - 40, 40 * sizeof(float));      change from 40 to 80

FIRlowpass:
static float s_fir_lp_prev[10];      change to 20
for (int k = -4; k <= 4; k++)       change to 8     (9 tap to 17 tap for 4000 sps)
val = s_fir_lp_has_prev ? s_fir_lp_prev[10 + idx] : data[0];        change from 10 to 20
float w = expf(-0.5f * (k * k) / (2.2f * 2.2f));        change 2.2f to 4.4f
if (len >= 10)      change to 20
memcpy(s_fir_lp_prev, data + len - 10, 10 * sizeof(float));     change 10s to 20

signal_quality.cpp:
waveform snr:
metrics.snrDb = 22.0f;      change to 25        (+3dB theoretical gain for 4000 sps)
if (metrics.snrDb > 35.0f) metrics.snrDb = 35.0f;       change to 38        (+3)
metrics.snrAccuracy = (metrics.snrDb / 25.0f) * 100.0f;     change to 28        (+3)

qrs decoupled:
if (d < 1500.0)     change to 750       (4000 sps takes half the time)
metrics.motionArtifactIndex = (float)(avgBgDelta / 150.0);      change to 75